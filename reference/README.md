# Reference copy

Design and copy notes (`txt.md`, `colors.md`, `typography.md`) lived under `docs/` before GitHub Pages used that folder for the static build output. They were moved here so `npm run build` can write the site to `docs/` without overwriting them.
